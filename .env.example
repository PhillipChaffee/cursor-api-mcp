# Create at https://cursor.com/dashboard/api
# Prefer the narrowest scope that covers the tools you need
# (e.g. members:read for org members; user key for Cloud Agents reads).
CURSOR_API_KEY=crsr_xxxxxxxx

# Optional override (default https://api.cursor.com)
# CURSOR_API_BASE_URL=https://api.cursor.com
